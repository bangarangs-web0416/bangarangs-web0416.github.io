var postId = "<%= @post.id %>"

$('div[post_id_'+ postId + ']').html("<%= j render partial: 'comments/post_comments', locals: { post: @post }%>")
$('#author-name').val('')
$('#comment-content').val('')
